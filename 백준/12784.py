class Tree():
    # 트리 초기화
    def __init__(self, index, left, right, value):
        self.index = index
        self.left = left
        self.right = right
        self.value = value
    
    def insertNode(self, index, child, value):
        if self.index == None 

    

for _ in range(int(input())):
    n,m = map(int,input().split())
    